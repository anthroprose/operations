---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: centos-6.4
    driver_config:
      customize:
        memory: 2048

suites:
  - name: default
    run_list: role[operations]
    attributes:
